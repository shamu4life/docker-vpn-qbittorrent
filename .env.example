# .env

# ----------------------------------------------------
# -- Paths and System Settings
# ----------------------------------------------------
# User and Group ID to run containers as. Find yours by running `id` in the terminal.
PUID=1000
PGID=1000
# Your local timezone for consistent timestamps in logs.
TZ=America/New_York
# The base path on your host machine where all configuration folders will be stored.
CONFIG_PATH=./configs
# The path on your host machine for completed downloads.
DOWNLOADS_PATH=./downloads
# The path on your host machine for in-progress downloads.
INCOMPLETE_DOWNLOADS=./downloads/incomplete

# ----------------------------------------------------
# -- VPN Configuration (for Gluetun)
# ----------------------------------------------------
# The VPN protocol to use (set to 'wireguard' or 'openvpn').
VPN_TYPE=wireguard
# Set this to the provider you want to use (e.g., protonvpn, nordvpn, mullvad).
VPN_PROVIDER=protonvpn

# --- OpenVPN Settings (only used if VPN_TYPE=openvpn) ---
OPENVPN_USER=your_openvpn_username+pmp
OPENVPN_PASSWORD=your_openvpn_password
OPENVPN_PROTOCOL=udp

# --- WireGuard Settings (only used if VPN_TYPE=wireguard) ---
WIREGUARD_PRIVATE_KEY=your_wireguard_private_key
WIREGUARD_ADDRESSES=your_wireguard_interface_address/32

# --- NordVPN Settings ---
NORDVPN_OPENVPN_USER=your_nordvpn_openvpn_username
NORDVPN_OPENVPN_PASSWORD=your_nordvpn_openvpn_password
NORDVPN_WIREGUARD_PRIVATE_KEY=your_nordvpn_wireguard_private_key
NORDVPN_TECHNOLOGY=
NORDVPN_REGION=

# --- Mullvad Settings ---
MULLVAD_WIREGUARD_PRIVATE_KEY=your_mullvad_wireguard_private_key
MULLVAD_WIREGUARD_ADDRESSES=your_mullvad_wireguard_address/32
MULLVAD_WIREGUARD_PUBLIC_KEY=your_mullvad_wireguard_public_key

# --- General VPN Settings ---
# If 'on', Gluetun will only connect to servers that support port forwarding.
PORT_FORWARD_ONLY=on
# If 'on', enables the port forwarding feature.
VPN_PORT_FORWARDING=on
# Filters the VPN server selection to specific countries (comma-separated).
SERVER_COUNTRIES=United States
# If 'on', disables all IPv6 traffic within the VPN container.
DISABLE_IPV6=on

# ----------------------------------------------------
# -- Application Ports
# ----------------------------------------------------
# The host ports to access the Web UIs for your services.
QBIT_WEBUI_PORT=8090
JACKETT_PORT=9117
FLARESOLVERR_PORT=8191
FILEBROWSER_PORT=8081

# ----------------------------------------------------
# -- qBittorrent Integration
# ----------------------------------------------------
# The username for the qBittorrent Web UI.
QBITTORRENT_USER=your_qbit_username
# The password for the qBittorrent Web UI.
QBITTORRENT_PASSWORD=your_qbit_password
# The raw Discord Webhook URL for the custom script's success/error notifications.
SCRIPT_NOTIFY_URL=https://discord.com/api/webhooks/your_id/your_token

# ----------------------------------------------------
# -- Watchtower Notifications
# ----------------------------------------------------
# The Shoutrrr-formatted URL for Watchtower's Discord notifications.
WATCHTOWER_NOTIFICATION_URL=discord://your_watchtower_token@your_watchtower_webhook_id
# The verbosity of notifications (debug, info, warn).
WATCHTOWER_NOTIFICATION_LEVEL=debug
# A custom title prepended to all Watchtower notifications.
WATCHTOWER_NOTIFICATION_TITLE_TAG="qBittorrent Stack"
# The 6-field cron expression for when to check for updates.
WATCHTOWER_SCHEDULE="0 30 15 * * *"
